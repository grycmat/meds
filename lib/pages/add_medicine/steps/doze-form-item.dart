import 'package:flutter/material.dart';

class DozeFormItem extends StatelessWidget {
  const DozeFormItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
