import 'package:flutter/material.dart';

class StepAdditionalInfo extends StatelessWidget {
  const StepAdditionalInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
